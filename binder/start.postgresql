#!/bin/bash

# Start PostgreSQL service
sudo service postgresql start > ~/logs/postgresql.log

# Wait for PostgreSQL to start
sleep 10

# Create user and database
sudo -u postgres psql -c "CREATE USER $USER;"
